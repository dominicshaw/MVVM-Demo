<Window x:Class="WpfApplication3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication3"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" d:DataContext="{d:DesignInstance local:MainViewModel}">

    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type local:Car}">
                <local:CarControl />
            </DataTemplate>
            <DataTemplate DataType="{x:Type local:Truck}">
                <local:TruckControl />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" x:Name="grd" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle, Mode=TwoWay}" />
        <ContentPresenter Grid.Row="1" Content="{Binding SelectedVehicle, Mode=TwoWay}"></ContentPresenter>
        <TextBlock Grid.Row="2" Text="{Binding Path=SelectedVehicle}"></TextBlock>
    </Grid>
    
</Window>
